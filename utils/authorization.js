// import { decodeToken } from "./token.js";

// const checkAuthorization = (header) => {
//   if (header.hasOwnProperty("authorization")) {
//     let token = decodeToken(header.authorization);
//     return token ? true : false;
//   }
// };

// export default checkAuthorization;